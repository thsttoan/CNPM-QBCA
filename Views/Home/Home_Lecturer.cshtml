@{
    ViewBag.Title = "Dashboard - Lecturer";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
    var roleKey = ViewBag.RoleKey ?? "lecturer";
}

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const userRole = "@roleKey";
        loadSidebarByRole(userRole);
        loadDashboardHtml(userRole);
    });
</script>
