@{
    ViewBag.Title = "Dashboard - Head of Department";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
    var roleKey = ViewBag.RoleKey ?? "head-dept";
}

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const userRole = "@roleKey";
        loadSidebarByRole(userRole);
        loadDashboardHtml(userRole);
    });
</script>
