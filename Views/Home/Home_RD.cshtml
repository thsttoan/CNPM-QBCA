@{
    ViewBag.Title = "Dashboard - R&D Staff";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
    var roleKey = ViewBag.RoleKey ?? "rd-staff";
}

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const userRole = "@roleKey";
        loadSidebarByRole(userRole);
        loadDashboardHtml(userRole);
    });
</script>
